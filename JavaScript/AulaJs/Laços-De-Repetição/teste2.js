let contador = 0;

//executa o bloco de código antes de verificar a condição de parada
do{
    console.log(contador);
    contador++;

    //verifica a condição de parada após a execução do código
}while (contador < 10);